services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development
      SECRET_KEY_BASE: dev_secret_key_lab3_not_for_production
    volumes:
      - .:/app
      - db_data:/app/storage
    stdin_open: true
    tty: true

volumes:
  db_data:
